version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: spam-classifier-api
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models

  frontend:
    build:
      context: .
      dockerfile: app/Dockerfile
    container_name: spam-classifier-frontend
    ports:
      - "8501:8501"
    depends_on:
      - api